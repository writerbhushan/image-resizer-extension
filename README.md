# image-resizer-extension
A lightweight Chrome extension that batch processes images by resizing them using print dimensions (inches × DPI) and exporting them in optimized formats. It supports bulk handling, automatic renaming, and optional ZIP packaging for efficient documentation workflows.
